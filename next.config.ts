import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['us-east-1-shared-usea1-02.graphassets.com', 'aceternity.com'],
  },
};

export default nextConfig;
